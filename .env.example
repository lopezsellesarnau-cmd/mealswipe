# Para probar en un dispositivo físico (no simulador): la IP de tu Mac en la
# red local, no "localhost" — el teléfono no puede resolver "localhost" como
# tu ordenador. Averíguala con: ipconfig getifaddr en0
EXPO_PUBLIC_API_URL=http://192.168.1.23:4001
